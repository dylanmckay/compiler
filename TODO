
lang::Function::basic_blocks() -> blocks()
  - Also rename BasicBlock to Block

Remove unnecessary trait bounds

Write tests for passes

Create an ir::instructions::BinaryOp trait with `fn operands(self) -> (Value,Value)` function.
